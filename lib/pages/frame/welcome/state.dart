//1. state class which holds the state variables (which are reactive)
class WelcomeState {
  //if welcome page has any variables which
  //have any state, those variables will be held in this class
}
