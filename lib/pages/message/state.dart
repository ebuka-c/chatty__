//state class which holds the state variables (which are reactive)
import 'package:chatty__/common/entities/entities.dart';
import 'package:get/get.dart';

class MessageState {
  var head_detail = UserItem().obs;
}
